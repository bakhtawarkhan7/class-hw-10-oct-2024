// components/Header.tsx

import styles from './Header.module.css';

const Header = () => {
  return (
    <header className={styles.header}>
      <h1>Welcome to Bakhtawar Khan's Portfolio</h1>
    </header>
  );
};

export default Header;
